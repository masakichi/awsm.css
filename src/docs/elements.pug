include _mixins.pug

+page("elements")
  p
    | There are demos of library capabilities. From skeleton to media.

  p
    | If these're not enough take a look more
    = ' '
    a(href="#moar") at the end of the page
    | .

  section
    +h("contents") Contents

    nav
      ul
        li
          a(href="#main") Main
        li
          a(href="#lists") Lists
        li
          a(href="#tables") Tables
        li
          a(href="#inlines") Inlines
        li
          a(href="#media") Media
        li
          a(href="#forms") Forms
        li
          a(href="#code") Code
        li
          a(href="#misc") Misc.
        li
          a(href="#moar") MOAR!

  section
    +h("main") Main

    p
      | awsm.css contains styles for all really useful
      =' '
      abbr(title="HyperText Markup Language is the standard markup language for creating web pages and web applications.") HTML5
      = ' '
      | skeleton tags.
      | If you open the source code of this page you'll see all of them.

    section
      +h("others", 3) What about others?

      figure
        img(src="images/cat.jpg", alt="Kitty")

        figcaption
          code figure
          = ' '
          | with kitten for your pleasure

      p
        code div
        = ' '
        | isn't styled because it doesn't have semantic value (as well as
        = ' '
        code span
        = ' '
        | elements which aren't styled either).

      aside
        p
          strong N.B.
          = ' '
          a(
            href="http://html5doctor.com/cite-and-blockquote-reloaded/",
            title="An article on html5doctor.com explains the right way of using quotes in HTML5",
            target="_blank"
          ) cite and blockquote – reloaded

      p But for example quotes are really nice:

      blockquote
        p See, you not only have to be a good coder to create a system like Linux, you have to be a sneaky bastard too.

        footer
          | —
          = ' '
          cite Linus Torvalds

      p Yeah! They almost have no custom styles :)

  section
    +h("lists") Lists

    p There's boring unordered list of jedi masters:

    ul
      li Obi-Wan Kenobi
      li Luke Skywalker
      li Yoda

    p Let's rank them!

    ol
      li Luke Skywalker
      li Yoda
      li Obi-Wan Kenobi

    p And we also have description lists:

    dl
      dt Blizzard
      dd A howling blizzard is summoned to strike the opposing team. It may also freeze them solid.

      dt Hidden Power
      dd A unique attack that varies in type depending on the Pokémon using it.

      dt Waterfall
      dd The user charges at the target and may make it flinch. It can also be used to climb a waterfall.

  section
    +h("tables") Tables

    p Let's look at Apple, Microsoft & Google:

    table
      thead
        tr
          th Title
          th Revenue
          th Number of employees
          th CEO
          th Founders

      tbody
        tr
          td Apple
          td US$ 215.639 billion
          td 115,000
          td Tim Cook
          td Steve Jobs, Steve Wozniak, Ronald Wayne

        tr
          td Microsoft
          td US$ 85.32 billion
          td 114,000
          td Satya Nadella
          td Bill Gates, Paul Allen

        tr
          td Google
          td US$ 74.54 billion
          td 57,100
          td Sundar Pichai
          td Larry Page, Sergey Brin

      tfoot
        tr
          td(colspan="5") Total revenue: US$ 375.499 billion

    p This table looks fine on mobile. Resize browser window and check this out!

  section
    +h("inlines") Inlines

    p
      img(src="images/surprised-cat.jpg", alt="Surprised cat")
      | The library has styles for
      = ' '
      strong strong
      | ,
      = ' '
      em emphasized
      = ' '
      | text and so on.

    p
      | Please note if you insert image inside
      = ' '
      code p
      = ' '
      | it'll be aligned to the right side.
      | But if you insert image outside
      = ' '
      code p
      = ' '
      | it'll be aligned to the center. See example below.

    hr

    p There are many others stylized inline tags in lib, but it would be boring to list them all.

    img(src="images/sleepy-cat.jpg", alt="Sleepy cat")

  section
    +h("media") Media

    p View of audio and video depends on your browser.

    aside
      p
        | This is a background theme of
        = ' '
        a(
          href="https://radio-t.com",
          title="The famous Russian IT-podcast",
          target="_blank"
        ) Radio-T
        | .

    audio(src="http://igoradamenko.com/github/awsm.css/radio-t.mp3", controls, loop)
      | It seems like your browser can't play this audio.

    video(src="http://igoradamenko.com/github/awsm.css/cats.mp4", controls)
      | It seems like your browser can't play this video.

  section
    +h("forms") Forms

    script.
      function showOutput() {
        document.getElementById('output').style.display = 'block';
        document.getElementById('reset').disabled = false;
      }

      function hideOutput() {
        document.getElementById('output').style.display = 'none';
        document.getElementById('reset').disabled = true;
      }

    form(onsubmit="showOutput(); return false;", onreset="hideOutput()")
      h4 Mars vacation program

      fieldset
        legend Be first

        label(for="full-name") Full Name
        input#full-name(type="text", value="Elon Reeve Musk")

        p
          | Sex

          br

          label
            input(type="radio", name="sex", checked)
            | Male

          label
            input(type="radio", name="sex")
            | Female

        label(for="date") Date of birth
        input#date(type="date", value="1971-06-28")

        label(for="email") E-mail
        input#email(type="email", value="elon.musk@spacex.com")

        label(for="phone") Phone number
        input#phone(type="tel", placeholder="Numbers only, please")

        label(for="country") Country
        select#country
          option(value="china") China
          option(value="russia") Russia
          option(value="usa", selected) USA

        label(for="about") Tell us about yourself
        textarea#about

        label(for="photo") Photo
        input#photo(type="file")

        label(for="eyes-color")
          | Eyes color
          = ' '
          small (just kidding)
        input#eyes-color(type="color", value="#000000")

        label(for="password") Password
        input#password(type="password", placeholder="Use strong password!")

        label
          input(type="checkbox", checked)
          | I pass my soul to SpaceX.

        br

        input(type="submit", value="Submit")
        button#reset(type="reset", disabled) Reset

        output#output(style="display: none")
          p Welcome on board!

  section
    +h("code") Code

    +code-block
      -
        var code = '<h2>Code formatting</h2>\n' +
        '<p>If you need a good syntax highlight for your code, check the <a title="Good plugins" href="plugins.html#highlightjs-prismjs">Plugins</a> section.</p>'

      = code

    p
      | If you need a good syntax highlight for your code, check the
      = ' '
      a(title="Good plugins", href="plugins.html#highlightjs-prismjs") Plugins
      = ' '
      | section.

  section
    +h("misc") Misc.

    details
      summary Show me the magic
      p This simple spoiler doesn't work in Internet Explorer / Edge yet. Coming soon :)
      p But now you can star the repo, why not? ^_^
      p
        a(
          class="github-button",
          href="https://github.com/igoradamenko/awsm.css",
          data-icon="octicon-star",
          data-size="large"
          data-show-count="true"
          aria-label="Star igoradamenko/awsm.css on GitHub"
        ) Star

      script(src="https://buttons.github.io/buttons.js", async, defer)

    noscript
      | This is an example of noscript but it seems like you'll never see that :)

  section
    +h("moar") MOAR!

    p Do you need more examples? Here they are:

    ul
      li
        a(href="examples/blog/") Blog

      li
        a(href="examples/homepage/") Simple homepage

      li
        strong Project's page
        = ' '
        | — just look at this site :)

      li
        | How page looks without awsm.css?
        = ' '
        a(href="javascript:toggleToggler()") Check this out!
        span(id="togglerNotice")
          = ' '
          | For removing floating button just click on the link once again.

      li
        | Find more examples on
        = ' '
        a(href="https://github.com/igoradamenko/awsm.css#examples") GitHub

  button#toggler Disable CSS

  script.
    var currentThemeHref;
    var toggler = document.getElementById('toggler');
    var togglerNotice = document.getElementById('togglerNotice');
    var themeChanger = document.getElementById('theme-changer');

    function toggle() {
      var themeNode = document.getElementById('theme');
      
      if (isCSSEnabled) {
        themeNode.removeAttribute('href');
        toggler.textContent = 'Enable CSS';
      } else {
        themeNode.setAttribute('href', currentThemeHref);
        toggler.textContent = 'Disable CSS';
      }

      isCSSEnabled = !isCSSEnabled;
    };

    function toggleToggler() {
      if (toggler.style.display !== 'block') {
        isCSSEnabled = true;
        currentThemeHref = document.getElementById('theme').getAttribute('href')

        toggler.addEventListener('click', toggle);

        toggler.style.display = 'block';
        togglerNotice.style.display = 'inline';
        themeChanger.setAttribute('disabled', true);
        
        toggle();
      } else {
        updateThemeHref(currentThemeHref);

        toggler.style.display = 'none';
        togglerNotice.style.display = 'none';
        themeChanger.removeAttribute('disabled');
      }
    }

  style.
    #togglerNotice {
      display: none;
      color: #b00000;
    }

    #toggler {
      display: none;
      position: fixed;
      top: 50%;
      right: 1em;
      transform: translateY(-50%);
    }
